name: onboarding
description: Coopay Cuatoemr OnBording Applications.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  splashscreen:
#  http: ^0.13.1
  floor: ^1.0.1
  awesome_dialog: ^1.3.2
  progress_dialog: ^1.2.4
  shared_preferences: ^2.0.5
  flutter_dropdown: ^0.0.8
  flutter_datetime_picker: ^1.5.1
  flutter_cupertino_date_picker: ^1.0.26+2

#  Image caturing

  image_picker: ^0.7.4
#  multipart_request: ^0.3.1
#  path_provider:
#  http:
#  camera:
  flutter_uploader: ^1.2.1




dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
  assets:
    - images/
